<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="checkUpdateDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="loading_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- <item name="android:backgroundDimEnabled">true</item> 定义灰色透明背景 -->
        <item name="android:backgroundDimAmount">0.2</item>
        <!-- android:backgroundDimAmount就是用来控制灰度的值 dialog以外的区域是黑色的，完全看不到其他内容，系统的默认值是0.5 -->

    </style>

    <style name="UnifiedLFDialogTheme" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <!-- 对话框是否浮动 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 对话框无标题 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 对话框是否透明 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 对话框是否有遮盖 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 背景昏暗是否启用 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 除去背景色 -->
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
